import React, { useState } from "react";
import Movie from "./Movie";
function MovieList() {
  return (
    <div>
      {/* {movies.map((movie) => {
        return (
          <Movie
            key={movie.id}
            name={movie.name}
            price={movie.price}
            id={movie.id}
          />
        );
      })} */}
    </div>
  );
}

export default MovieList;
